#include <stdio.h>
#include "header/functs.h"

void main()
{
    entry();
	/*FILE* fp = fopen("source/M.ind", "wb");
    if (fp == NULL)
    {
        printf("Unable to create file.\n");
    }
	printf("Hello from C!\n");*/
}